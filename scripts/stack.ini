[supervisord]
logfile=logs/supervisord.log
pidfile=scripts/supervisord.pid
childlogdir=logs

[program:database]
command     = pivot -s schema.json web
autorestart = true
directory   = database

[program:backend]
command     = ./server.py
autorestart = true
directory   = backend

[program:frontend]
command     = diecast
autorestart = true
directory   = frontend
